{/* Badge.mdx */}

import { Canvas, Meta, Story, Controls } from '@storybook/blocks';
import { Badge } from './badge';
import * as BadgeStories from './badge.stories';

<Meta of={BadgeStories} />

# Badge Component

The `Badge` component is used to display a badge with various variants and colors. It is highly customizable and can be used to highlight important information or statuses.

## Table of Contents

- [Badge Component](#badge-component)
  - [Default Badge](#default-badge)
  - [All Variants](#all-variants)

# Default Badge
<Canvas>
  <Story of={BadgeStories.Default} />
</Canvas>

<Controls of={BadgeStories.Default} />

```jsx
import React from 'react';

export const Button = ({ children, onClick }) => (
  <button
    className="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"
    onClick={onClick}
  >
    {children}
  </button>
);
```

# All Variants
<Canvas of={BadgeStories.AllVariants} />

